# Runtime Configuration Governance

## Evidence-backed loading rules

| Variable or group | Use location | Loading actor | Precedence | Local-development provision | Production provision | Owner | Evidence | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `APP_NAME`, `APP_VERSION`, `DEBUG` | `backend/config.py`; `DEBUG` is also declared by both compose files | FastAPI process configuration reads process environment | Direct process environment value, otherwise code default | Shell/process environment or compose declaration; automatic `.env` loading is UNKNOWN | UNKNOWN | UNKNOWN | Explicit `os.getenv` lookups; compose environment declarations | CONFIRMED for lookup; UNKNOWN for production provision and owner |
| `DAILY_EXP_CAP`, `ANTI_FARMING_INTERVAL_MINUTES` | `backend/config.py`; integrity scripts validate their safe defaults | FastAPI process configuration | Direct process environment value, otherwise code default | Shell/process environment; WP-0004 template records safe non-secret defaults | UNKNOWN | UNKNOWN | Explicit lookups in `backend/config.py`; `scripts/check_canonical_constants.py`; read-only WP-0004 template | CONFIRMED for lookup and local template; UNKNOWN for production provision and owner |
| `SQLALCHEMY_DATABASE_URL`, `DATABASE_URL` | `backend/config.py`, consumed by `backend/database.py` | FastAPI process configuration and SQLAlchemy engine setup | `SQLALCHEMY_DATABASE_URL` is checked first, then `DATABASE_URL`; if neither yields a value, component DB keys are used | Shell/process environment; WP-0004 template provides blank names only | UNKNOWN | UNKNOWN | `backend/config.py` override logic; `backend/database.py` engine setup; read-only WP-0004 template | CONFIRMED for code precedence; UNKNOWN for production provision and owner |
| `DB_HOST`, `DB_PORT`, `DB_NAME`, `DB_USER`, `DB_PASSWORD` | `backend/config.py`; backend environment declarations in both compose files | FastAPI process configuration; compose passes container environment | Used to construct the component database configuration when URL overrides are absent | Compose environment declaration or shell/process environment; WP-0004 template values are blank | UNKNOWN | UNKNOWN | `backend/config.py`, `docker-compose.yml`, `deploy/docker-compose.web.yml`, read-only WP-0004 template | CONFIRMED for use and fallback position; UNKNOWN for production provision and owner |
| `POSTGRES_DB`, `POSTGRES_USER`, `POSTGRES_PASSWORD` | PostgreSQL service environment in both compose files | Compose/container runtime | Compose-provided container environment; no application-code precedence found | Compose environment declaration; WP-0004 template values are blank | UNKNOWN | UNKNOWN | `docker-compose.yml`, `deploy/docker-compose.web.yml`, read-only WP-0004 template | CONFIRMED for compose use; UNKNOWN for application use, production provision, and owner |
| `API_BASE_URL` | `lib/api_config.dart`, `lib/api_config_io.dart`, `lib/api_config_web.dart`, `lib/api_client.dart`; README launch guidance | Flutter compile-time environment via `String.fromEnvironment` | Explicit `--dart-define=API_BASE_URL` when non-empty; otherwise platform-aware fallback resolver | Pass `--dart-define` to Flutter launch; WP-0004 template records the name only and is not a loader | UNKNOWN | UNKNOWN | Flutter `String.fromEnvironment`, API client/config files, README guidance, read-only WP-0004 template | CONFIRMED for loading and precedence; UNKNOWN for production provision and owner |

## Cross-cutting rules

- No `dotenv`, `load_dotenv`, `env_file`, or equivalent repository loader was found. A `.env.example` file is therefore a configuration contract, not proof that local `.env` is automatically loaded.
- The root `.env.example` is present on the read-only WP-0004 branch but is not yet present on `main`. WP-0005 does not modify it.
- No GitHub Actions, GitLab CI, Azure Pipelines, or repository secret-tool configuration was found: **CI 미구성 또는 저장소 근거 없음**.
- Do not place local or production values in repository files, documentation, commits, or PR text.

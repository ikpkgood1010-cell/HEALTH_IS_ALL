# Codex Handover — WP-0009

Work on `codex/wp-0009-supabase-db-connection`. Allowed changes are backend DB configuration, migration runner, related tests, minimal dependencies, `.env.example` only if `DATABASE_URL` is absent, and WP-0009 documentation. Never execute a real migration or store real connection data.
